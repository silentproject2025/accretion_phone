name: Build Firmware Binaries

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Update Board Manager and Install Core & Libraries
        run: |
          arduino-cli config init --overwrite
          arduino-cli config set board_manager.additional_urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli config set library.enable_unsafe_install true
          arduino-cli core update-index
          arduino-cli core install esp32:esp32

          arduino-cli lib install "LovyanGFX"
          arduino-cli lib install "JPEGDEC"
          # PENTING: pin ke versi 8.x -- tanpa pin, Library Manager bisa
          # ngasih LVGL v9.x yg API-nya beda total (lv_disp_drv_t dkk sudah
          # diganti di v9), padahal kode di repo ini ditulis utk API v8.3.x.
          arduino-cli lib install "lvgl@8.3.11"
          # Coba install dari Library Manager, kalau gagal fallback ke git
          arduino-cli lib install "ESP32-audioI2S-master@3.4.6" || \
            arduino-cli lib install --git-url https://github.com/schreibfaul1/ESP32-audioI2S.git

          arduino-cli lib install "EspUsbHost" || \
            arduino-cli lib install --git-url https://github.com/tanakamasayuki/EspUsbHost.git

      - name: Compile Sketch
        run: |
          mkdir -p build_sketch
          # Wildcard -- lebih tahan lama drpd nyebutin nama file satu-satu,
          # krn bakal terus nambah file .h/.cpp tiap app yg di-port ke LVGL.
          cp *.ino *.h *.cpp build_sketch/ 2>/dev/null || true
          mv build_sketch/phone.ino build_sketch/build_sketch.ino
          arduino-cli compile --fqbn esp32:esp32:esp32s3 \
            --board-options "FlashSize=16M,PSRAM=opi,PartitionScheme=app3M_fat9M_16MB" \
            --build-property "compiler.cpp.extra_flags=-DLV_CONF_INCLUDE_SIMPLE=1 -DCONFIG_MBEDTLS_DYNAMIC_BUFFER=1 -DCONFIG_MBEDTLS_ASYMMETRIC_CONTENT_LEN=1 -DCONFIG_MBEDTLS_SSL_IN_CONTENT_LEN=4096 -DCONFIG_MBEDTLS_SSL_OUT_CONTENT_LEN=2048" \
            --build-property "compiler.c.extra_flags=-DLV_CONF_INCLUDE_SIMPLE=1" \
            build_sketch --output-dir build

      - name: Upload Binary Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: esp32-firmware-binaries
          path: build/*.bin
